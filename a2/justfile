build:
    ./gradlew clean build

clean:
    rm -f *.log*
    rm -f *.lck*

run player_num:
    just build && \
    ./comp512st/tiapp/run_tiapp.sh {{player_num}}

kill:
    pkill -f "comp512p2.jar" || true

auto-test:
    just kill && \
    just build && \
    ./comp512st/tests/run_tiapp_auto.sh

deploy:
    just build
    ./scripts/deploy.sh

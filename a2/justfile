build:
    ./gradlew clean build

run player_num:
    source .env && \
    ./comp512st/tiapp/run_tiapp.sh {{player_num}}

clean:
    rm -f *.log*
    rm -f *.lck*

run-nuke:
    -kill -9 $(lsof -ti:40122,40222,40322)
    rm -f *.log*
    rm -f *.lck*
    ./gradlew clean build
